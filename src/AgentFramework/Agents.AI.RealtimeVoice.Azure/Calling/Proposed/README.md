# Contact Center SDK

## NOTES
- Make sure the input & output formats align between the speech synthesizer and recognizer. For example, if the synthesizer outputs 24KHz audio, the recognizer should be configured to expect 24KHz audio as well.
