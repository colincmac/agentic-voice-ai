# yaml-language-server: $schema=https://aka.ms/teams-toolkit/v1.7/yaml.schema.json
# Visit https://aka.ms/teamsfx-v5.0-guide for details on this file
# Visit https://aka.ms/teamsfx-actions for details on actions
version: v1.7

provision:
  # Creates a Teams app
  - uses: teamsApp/create
    with:
      # Teams app name
      name: Call Agent (${{APP_NAME_SUFFIX}})
    # Write the information of created resources into environment file for
    # the specified environment variable(s).
    writeToEnvironmentFile:
      teamsAppId: TEAMS_APP_ID

  # - uses: aadApp/create
  #   with:
  #     name: Call Agent (${{APP_NAME_SUFFIX}})
  #     generateClientSecret: true
  #     signInAudience: "AzureADMultipleOrgs"
  #   writeToEnvironmentFile:
  #     # The Microsoft Entra application's client id created for bot.
  #     clientId: SECRET_AUTH_CLIENT_ID
  #     # The Microsoft Entra application's client secret created for bot.
  #     clientSecret: SECRET_AUTH_CLIENT_SECRET
  #     # The Microsoft Entra application's object id created for bot.
  #     objectId: SECRET_AUTH_CLIENT_OID

  # # Generate runtime appsettings to JSON file
  # - uses: file/createOrUpdateJsonFile
  #   with:
  #     target: ../src/Agents/Showcase.AgentHost/appsettings.Development.json
  #     content:
  #       BOT_TYPE: 'MultiTenant'
  #       BOT_ID: ${{SECRET_AUTH_CLIENT_ID}}
  #       BOT_PASSWORD: ${{SECRET_AUTH_CLIENT_SECRET}}
  #       TokenValidation:
  #         Audiences:
  #           ClientId: ${{SECRET_AUTH_CLIENT_ID}}
  #       Connections:
  #         BotServiceConnection:
  #           Settings:
  #             AuthType: "ClientSecret"
  #             AuthorityEndpoint: "https://login.microsoftonline.com/${{SECRET_AUTH_TENANT_ID}}"
  #             ClientId: ${{SECRET_AUTH_CLIENT_ID}}
  #             ClientSecret: ${{SECRET_AUTH_CLIENT_SECRET}}
  #             TenantId: ${{SECRET_AUTH_TENANT_ID}}


  # Create or update the bot registration on dev.botframework.com
  # - uses: botFramework/create
  #   with:
  #     botId: ${{SECRET_AUTH_CLIENT_ID}}
  #     name: Showcase.TeamsBot
  #     messagingEndpoint: ${{BOT_ENDPOINT}}/api/messages
  #     description: ""
  #     channels:
  #       - name: msteams

  - uses: aadApp/update # Apply the AAD manifest to an existing AAD app. Will use the object id in manifest file to determine which AAD app to update.
    with:
      manifestPath: ./appPackage/aad.manifest.json 
      outputFilePath: ./build/aad.manifest.${{TEAMSFX_ENV}}.json


      # Validate using manifest schema
  - uses: teamsApp/validateManifest
    with:
      # Path to manifest template
      manifestPath: ./appPackage/manifest.json

  # Build Teams app package with latest env value
  - uses: teamsApp/zipAppPackage
    with:
      # Path to manifest template
      manifestPath: ./appPackage/manifest.json
      outputZipPath: ./appPackage/build/appPackage.${{TEAMSFX_ENV}}.zip
      outputFolder: ./appPackage/build

  # Validate app package using validation rules
  - uses: teamsApp/validateAppPackage
    with:
      # Relative path to this file. This is the path for built zip file.
      appPackagePath: ./appPackage/build/appPackage.${{TEAMSFX_ENV}}.zip

  # Apply the Teams app manifest to an existing Teams app in
  # Developer Portal.
  # Will use the app id in manifest file to determine which Teams app to update.
  - uses: teamsApp/update
    with:
      # Relative path to this file. This is the path for built zip file.
      appPackagePath: ./appPackage/build/appPackage.${{TEAMSFX_ENV}}.zip
